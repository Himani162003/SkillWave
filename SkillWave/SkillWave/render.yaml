services:
  - type: web
    name: my-django-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn SkillWave.wsgi
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: SkillWave.settings
      - key: SECRET_KEY
        generateValue: true
